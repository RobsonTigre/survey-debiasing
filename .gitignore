# Companion repo for the Substack post — keep only R/Python scripts, README,
# and this file. Everything else (design docs, draft posts, PDFs, .DS_Store, etc.)
# lives locally for reference but is not tracked.

/*
!/*.R
!/*.py
!/README.md
!/.gitignore
